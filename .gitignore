__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/
*.egg-info/
build/
dist/

# Real device backups from your own lab. Only the example CSV and the
# small sample backups under examples/ are meant to be committed.
/backups/

# Never commit real credentials. Copy .env.example to .env and fill
# it in locally, then `export $(grep -v '^#' .env | xargs)` before
# running the tool (see RUNBOOK.md).
.env
