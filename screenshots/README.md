# Screenshots

Real screenshots go in this folder, replacing this placeholder file, once RUNBOOK.md has actually been walked through end to end. Filenames below match what README.md, RUNBOOK.md, and BLOG-POST.md already link to.

- [ ] `01-containerlab-deploy.png` - `containerlab deploy` output showing both lab nodes up with their management IPs (RUNBOOK.md Part 3).
- [ ] `02-pytest-passing.png` - `pytest -v` output showing all tests passing (RUNBOOK.md Part 5).
- [ ] `03-first-run-baseline.png` - first `configbackup` run against the fresh lab, showing "first backup saved" for both devices (RUNBOOK.md Part 8).
- [ ] `04-change-detected.png` - second run after changing `lab-r1`'s hostname, showing `lab-r1` as CHANGED and `lab-r2` as no change (RUNBOOK.md Part 9).
- [ ] `05-alert-email.png` - the alert email itself, showing the diff in the message body (RUNBOOK.md Part 9).

Once these are captured, delete the TODO checklist above and this note, and the markdown image tags already in README.md, RUNBOOK.md, and BLOG-POST.md will just work since they already point at these exact filenames.
